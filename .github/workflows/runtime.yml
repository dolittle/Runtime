name: Runtime

on:
  push:
    branches:
      - "**"

jobs:
  ci:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-dotnet@v1
        with:
          dotnet-version: "6.0.x"
      - name: Build
        run: dotnet build --configuration Release
      - name: Test
        run: dotnet test --no-build --configuration Release --collect:"XPlat Code Coverage"
      - name: Report code coverage
        env:
          CODACY_PROJECT_TOKEN: ${{ secrets.CODACY_PROJECT_TOKEN }}
        run: bash <(curl -Ls https://coverage.codacy.com/get.sh) report -l CSharp $(find . -name coverage.cobertura.xml -printf '-r %p ')

  build-prod-docker-image:
    name: Build Production Docker Image
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build Production Docker image
        uses: docker/build-push-action@v2
        with:
          push: false
          context: .
          file: ./Docker/Production/Dockerfile
          build-args: |
            VERSION=0.0.0

  build-dev-docker-image:
    name: Build Development Docker Image
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build Development Docker image
        uses: docker/build-push-action@v2
        with:
          push: false
          context: .
          file: ./Docker/Development/Dockerfile
          build-args: |
            VERSION=0.0.0

  build-arm64-dev-docker-image:
    name: Build ARM64 Development Docker Image
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - uses: actions/checkout@v2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build ARM64 Development Docker image
        uses: docker/build-push-action@v2
        with:
          push: false
          context: .
          file: ./Docker/ARM64Development/Dockerfile
          build-args: |
            VERSION=0.0.0
          platforms: linux/arm64

  build-arm64-prod-docker-image:
    name: Build ARM64 Production Docker Image
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - uses: actions/checkout@v2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build ARM64 Production Docker image
        uses: docker/build-push-action@v2
        with:
          push: false
          context: .
          file: ./Docker/ARM64Production/Dockerfile
          build-args: |
            VERSION=0.0.0
          platforms: linux/arm64
